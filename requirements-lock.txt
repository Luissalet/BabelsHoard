# Exact, reproducible install for Babel's Hoard.
# Generated on Linux/cp311 for the runtime deps in pyproject.toml; every
# package here ships cp313 win_amd64 wheels (checked against PyPI at pin
# time) so `pip install -r requirements-lock.txt` works unchanged on the
# Windows/py3.13 production target.
#
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements-lock.txt      (Windows)
#   .venv/bin/pip install -r requirements-lock.txt           (Linux/macOS)

# --- direct dependencies ---
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
griffe==2.3.0
httpx==0.28.1
mcp==1.30.0

# --- key transitive pins (resolved by the above; pinned for a reproducible lock) ---
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
h11==0.16.0
httpcore==1.0.9
httpx-sse==0.4.3
idna==3.20
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
packaging==26.3
pydantic-settings==2.15.0
pydantic_core==2.46.5
python-dotenv==1.2.3
python-multipart==0.0.32
PyJWT==2.14.0
referencing==0.37.0
rpds-py==2026.6.3
sse-starlette==3.4.11
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0

# --- Windows only ---
colorama==0.4.6 ; sys_platform == "win32"

# --- dev / test only (not needed to run the app) ---
pytest==9.1.1
pytest-asyncio==1.4.0
playwright==1.63.0
